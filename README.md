# Clockwork Builds

Generated browser builds for the private `cyscott/clockwork` Godot project.

This repository intentionally contains compiled Web exports only — not the Godot source project.

- `/dev/` — latest development build from the private `dev` branch
- `/prod/` — stable production build from the private `main` branch

The contents of this repository are replaced automatically by the private Clockwork build workflow.
